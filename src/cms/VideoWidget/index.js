import React from "react";

const VideoWidget = ({ value, field, forID, classNameWrapper, onChange }) => {
  console.log({
    componentLogging: "VideoWidget",
    value,
    field,
    forID,
    classNameWrapper,
    onChange,
  });
  return null;
};

export default VideoWidget;
